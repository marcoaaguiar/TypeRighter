There are many colors : % should fail for ' :'
red , blue, green... % should fail for ' ,' and '...'

A color is red . % Should fail for ' .'
Red is a color. 

Jetpaks are dangerous ; although I would love to have one ! % should fail for ' ;' and '!'